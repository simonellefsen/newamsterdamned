node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# build output
build/
.vercel/

# Generated voice packs (MP3s + local corpus dumps). Keep a tiny empty manifest in git.
static/voice/v1/lines/
static/voice/v1/corpus-*.json
.voice-out/
# Durable TTS cache — keeps API costs down across regenerate runs
.voice-cache/

# Generated Blender camera plates (regenerate with npm run art:plates)
tools/blender/plates/*.svg
tools/blender/plates/*.png
!tools/blender/plates/.gitkeep

# Local Blender working files (not a pack yet)
tools/blender/*.blend
tools/blender/*.blend1
